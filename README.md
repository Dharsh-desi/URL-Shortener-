# URL-Shortener-
This project is a frontend-based URL Shortener application called SnapLink. Users can paste a long URL, and the application generates a unique 6-character short URL. It also stores the history using localStorage, allows users to copy shortened links, and supports deleting or clearing history."
